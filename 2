#include <stdio.h>

int main() { int num[5]; int i, sum = 0;

for (i = 0; i < 5; i++) {
    printf("숫자를 입력하세요 : ");
    scanf_s("%d", &num[i]);

    sum += num[i];
}

printf("합은 %d 입니다.", sum);

return 0;
}
